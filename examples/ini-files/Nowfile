[INI file decoding example]

Show how to decode INI files.

[commands/run]

path "test.ini" | read | ini.decode | as d
get $d "ini file" | as subd
get $subd with | as with
assert ($with == "comments inline")
